const SectionDiverText = ({ children }) => {
  return (
    <div className="col-span-12 flex justify-center items-center py-4 border-y mt-30 border-white font-dm-sans ">
      {children}
    </div>
  );
};

export default SectionDiverText;
